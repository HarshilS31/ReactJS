const JSXRules = () => {
  return (
    <div>
        <h1>
            JSX Rules
        </h1>
        <p className="text">JSX must return a single parent element</p>
        <p>JSX elements must be properly closed!</p>
        <p>JSX attributes are written using camelCase(preferred)!</p>
    </div>
  )
}

export default JSXRules